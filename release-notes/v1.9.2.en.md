- Fixed Alt+L / Alt+F keyboard shortcuts not working to start conversion or merge (regression caused by EVT_CHAR_HOOK on the main frame).
- Fixed file merge failing when a filename contains an apostrophe.
- Fixed audio copy mode being ignored during file merges (audio was always re-encoded).
- Fixed an orphaned FFmpeg process when image conversion exceeded the 120-second timeout.
- Fixed overly broad exception catches in conversion progress parsing and file probing.
- Removed a duplicate "report sent" message box in the Contact Support dialog.
