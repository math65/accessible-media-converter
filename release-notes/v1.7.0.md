# What's new in 1.7.0

- Added application language selection with `Automatic`, `French`, and `English`.
- Added automatic language detection with a clear label that shows the detected language.
- Added offline documentation in English, with language-aware fallback between `en` and `fr`.
- Refined video and audio output settings for clearer container-specific behavior.
- Improved the Windows release pipeline validation for multilingual offline documentation.
