- When a conversion fails, a dialog now appears automatically to offer sending an error report — no more manual debug mode activation.
- Removed debug mode entirely (menu, restart, session restore, log file).
- Fixed a rare issue where multiple error dialogs could appear simultaneously during a batch conversion.
- Fixed garbled accents in the console log on Windows.
