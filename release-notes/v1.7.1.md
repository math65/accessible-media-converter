# What's new in 1.7.1

- Refreshed the embedded `FFmpeg` and `FFprobe` binaries in the Windows build.
- Kept the application behavior unchanged while republishing the installer with the newer media tools.
